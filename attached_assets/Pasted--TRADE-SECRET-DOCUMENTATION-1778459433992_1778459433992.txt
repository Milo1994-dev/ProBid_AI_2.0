========================================================================
TRADE-SECRET DOCUMENTATION REQUEST — ProBid AI Estimating Logic
========================================================================

CONTEXT
-------
We are creating a legally-defensible trade-secret record of the AI
estimating logic that lives in this repo (probidcore.net / "ProBid AI").
The record will sit in this repo at:

  docs/legal/trade-secrets/ai-estimating-logic.md

A blank template + a README explaining the legal framing already exist
in the sibling ProBidCore repo (probidcore.com) at:

  docs/legal/trade-secrets/README.md
  docs/legal/trade-secrets/ai-estimating-logic.template.md

Copy that template into THIS repo, then fill in every [FILL IN: ...]
placeholder using the information gathered below. Commit the filled-in
file with the message:

  Document AI estimating logic as trade secret

The git commit timestamp + author becomes the legally-relevant date
record under the Defend Trade Secrets Act (18 U.S.C. § 1836).

⚠️  TWO THINGS TO DO BEFORE GATHERING ANYTHING ⚠️
   1. Confirm this repo is PRIVATE (not Public, not Unlisted). If it is
      public right now, make it private first — pasting prompts into a
      doc inside a public repo undermines the trade-secret claim.
   2. When pasting code below, REDACT every API key as
      `OPENAI_API_KEY=<REDACTED>` (and the same for any other secrets).
      Do not commit real keys to the doc.

========================================================================
INFORMATION TO GATHER
========================================================================

──────────────────────────────────────────────────────────────────────
BLOCK 1 — Repo orientation
──────────────────────────────────────────────────────────────────────
[ ] Output of `ls` at the repo root
[ ] Output of `ls src/` (or whatever the source folder is called)
[ ] The package.json (or pyproject.toml / requirements.txt if Python)
    — needed to confirm which AI SDK is in use (openai,
    @anthropic-ai/sdk, @google/generative-ai, etc.)
[ ] Repo host: GitHub / GitLab / Replit / Bitbucket / other
[ ] Repo visibility right now: Private or Public?

──────────────────────────────────────────────────────────────────────
BLOCK 2 — The LLM call (heart of the trade secret)
──────────────────────────────────────────────────────────────────────
[ ] Full file contents of every file where the LLM is invoked.
    Likely names: estimateGenerator.ts, aiEstimator.ts,
    generateEstimate.ts, prompt.ts, llm.ts, openaiClient.ts
[ ] The SYSTEM PROMPT string — paste verbatim, including templating
    placeholders like {{projectType}}
[ ] The USER PROMPT TEMPLATE — verbatim
[ ] Any FEW-SHOT EXAMPLES baked into the prompt — verbatim
[ ] Model config:
      - model name (e.g. gpt-4o-2024-08-06, claude-3-5-sonnet)
      - temperature
      - top_p
      - max_tokens
      - response_format / structured-output schema if any
[ ] Retry / fallback behavior (e.g. "if model returns invalid JSON,
    retry once with stricter prompt")

──────────────────────────────────────────────────────────────────────
BLOCK 3 — Reference data / lookups (the moat)
──────────────────────────────────────────────────────────────────────
[ ] Every proprietary data file used to enrich or override LLM output.
    Look in: data/, reference/, seed/, fixtures/. Examples:
      - unit-cost table
      - regional price multipliers
      - WBS code dictionary
      - cost-type mapping
      - vector DB / embeddings index (Pinecone, pgvector, ChromaDB)
[ ] For each file, send:
      - filename
      - ~5 sample rows
      - total row count
      - where the data came from (e.g. "RSMeans 2024",
        "scraped from public bids", "hand-curated by founder over
        8 months")
[ ] The lookup code — the function that takes an LLM line item and
    enriches/overrides it with reference data

──────────────────────────────────────────────────────────────────────
BLOCK 4 — Pre-processing & post-processing
──────────────────────────────────────────────────────────────────────
[ ] Code that runs BEFORE the LLM call (input validation,
    classification, project-type detection)
[ ] Code that runs AFTER the LLM call:
      - JSON parsing / schema validation (Zod, JSON schema, etc.)
      - Deduplication of line items
      - Price-sanity checks ("flag anything > 3× reference price")
      - Quantity rounding rules
      - Auto-assignment of WBS codes or cost types

──────────────────────────────────────────────────────────────────────
BLOCK 5 — Inputs
──────────────────────────────────────────────────────────────────────
[ ] The HTTP route or function signature that kicks off estimation
    (e.g. POST /api/estimate in routes/estimate.ts) — just the
    handler is fine
[ ] Any hidden defaults (e.g. "if user doesn't specify region, we
    default to 'US-Northwest'")

──────────────────────────────────────────────────────────────────────
BLOCK 6 — Outputs
──────────────────────────────────────────────────────────────────────
[ ] The final response shape sent back to the user
[ ] Confirmation: does it match the public schema in
    PROBID_AI_INTEGRATION.md (in the ProBidCore repo)?
      { name, lineItems: [
          { description, quantity, unitCost, uom, wbsCode, costType }
      ]}
    — or does ProBid AI compute extra fields that get stripped before
    handoff to ProBidCore?

──────────────────────────────────────────────────────────────────────
BLOCK 7 — Reasonable secrecy measures (for §7 of the doc)
──────────────────────────────────────────────────────────────────────
Answer each:

[ ] Repo visibility: Private or Public?
[ ] Who has access right now? List names + roles, e.g.
    "Jesse Kirchner — owner; Acme Contractors LLC — read-only on
    src/prompts/"
[ ] Is 2FA enforced on every account with access?
    (Yes / No / "I think so but haven't checked")
[ ] Where are the LLM API keys stored?
    (Replit Secrets / GitHub Secrets / .env / Doppler / 1Password /
    "in the code")
[ ] Has anyone outside the owner ever seen the prompts or algorithm?
    If yes, did they sign an NDA, and when?
[ ] Do public marketing pages (probidcore.net, demo videos, sales
    decks) ever show the actual prompts, model name, or reference
    data? (Yes / No)
[ ] Have you ever been on a podcast, conference panel, or YouTube
    video where you described how the algorithm works in detail?
    (Yes / No)

──────────────────────────────────────────────────────────────────────
BLOCK 8 — Access list (for §8 of the doc)
──────────────────────────────────────────────────────────────────────
For everyone who has had access to either the prompts or the source:

[ ] Name
[ ] Role (founder / contractor / employee / advisor / investor /
    customer)
[ ] Access since (approx date)
[ ] NDA signed? (Yes — date, or No)
[ ] Still has access? (Yes / No — and if No, when revoked?)

──────────────────────────────────────────────────────────────────────
BLOCK 9 — Provenance / origin (gives the doc legal teeth)
──────────────────────────────────────────────────────────────────────
[ ] Roughly when was the current version of the prompt + reference
    data built? (e.g. "started Jan 2025, current version since
    March 2026")
[ ] How many iterations did you go through before settling on the
    current prompt? (Rough count — "20+ versions over 3 months")
[ ] Is any portion of the algorithm derived from a paper, blog post,
    or open-source project? (Yes — name it, or "all original work")

========================================================================
HOW TO SEND IT BACK
========================================================================

Paste all gathered information into a single message. For Blocks 2–6
(actual code), use code fences. For Blocks 1, 7, 8, 9 (metadata /
answers), write inline.

Anything that genuinely doesn't apply, mark "N/A" with a one-line
reason. Anything not yet known, mark "TBD" — better an honest gap than
a guess.

After you send the bundle back, the filled-in
docs/legal/trade-secrets/ai-estimating-logic.md will be drafted for
your review and commit.
========================================================================